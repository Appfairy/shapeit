import { default as vectorData } from './vector';
import { default as openPolygonData } from './open_polygon';
import { default as circleData } from './circle';

export default []
  .concat(vectorData)
  .concat(openPolygonData)
  .concat(circleData)
